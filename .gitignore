# Secrets — never commit credentials
*.env
secrets.env
**/secrets.env

# Tableau Hyper extracts (regenerated locally)
*.hyper

# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

.vercel

# Playwright MCP debug output (local verification only)
.playwright-mcp/
*-local-render.png
